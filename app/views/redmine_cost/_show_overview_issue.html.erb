<%= issue_fields_rows do |rows|
  if User.current.allowed_to?(:view_cost_entries, @project)
    costs = CostEntry.where(issue_id: @issue.id).map(&:costs).compact.sum
    rows.right l(:label_spent_cost), (costs > 0 ? link_to(costs, issue_cost_entries_path(@issue)) : "-"), :class => 'spent-costs'
  end
end %>